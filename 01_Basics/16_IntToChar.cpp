#include <iostream>
using namespace std;

int main(){

    int x;
    cout << "Enter an integer(ASCII) : ";
    cin >> x;

    cout << "Integer(ASCII) : " << x << endl;
    cout << "Character : " << (char)x << endl;

    return 0;
}